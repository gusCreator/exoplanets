const transitionColor = 'transition-colors duration-300 ease-out';
const defaultColor = `${transitionColor} text-primary hover:text-secondary`;
const invertedColor = `${transitionColor} text-secondary hover:text-primary`;

export { defaultColor, invertedColor };
